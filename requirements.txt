# UI Framework
PySide6>=6.5.0

# Build tools
pyinstaller>=6.0.0
pillow>=10.0.0  # Required for icon processing in PyInstaller

# API/Server dependencies
fastapi>=0.104.0
requests>=2.31.0
pydantic>=2.0.0

# LLM providers (optional - install as needed)
openai>=1.0.0  # For OpenAI integration
anthropic>=0.18.0  # For Anthropic Claude integration

# Note: sqlite3 and asyncio are built-in Python modules
# sqlglot>=23.0.0  # SQL parsing and validation
sqlglot>=23.0.0
