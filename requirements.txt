# UI Framework
PySide6>=6.5.0

# Build tools
pyinstaller>=6.0.0
pillow>=10.0.0  # Required for icon processing in PyInstaller

# API/Server dependencies
fastapi>=0.104.0
uvicorn>=0.24.0  # ASGI server for in-process FastAPI/MCP when frozen (server_fail_4 Solution A)
requests>=2.31.0
pydantic>=2.0.0

# LLM providers (optional - install as needed)
openai>=1.0.0  # For OpenAI integration
anthropic>=0.18.0  # For Anthropic Claude integration

# Note: sqlite3 and asyncio are built-in Python modules
# sqlglot>=23.0.0  # SQL parsing and validation
sqlglot>=23.0.0

# Data analysis
pandas>=2.0.0  # Data analysis and DataFrame support

# Visualization (NL Query results)
matplotlib>=3.7.0
seaborn>=0.12.0

# Security
cryptography>=41.0.0  # For API key encryption
